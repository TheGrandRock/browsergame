[package]
name = "browsergame"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "2.0.3" }
log = "0.4"
rustrict = { version = "0.7", default-features = false, features = ["censor"] }
